org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Opt-out del Kotlin integrado de AGP 9: usamos el plugin clásico
# org.jetbrains.kotlin.android (requerido por los subplugins compose y
# serialization). TODO(migrar): quitar estas líneas cuando pasemos a Kotlin
# integrado; son obligatorias en AGP 10.
android.builtInKotlin=false
android.newDsl=false
