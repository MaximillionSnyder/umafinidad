{
  "name": "umafinidad",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-gametora.mjs",
    "fetch:aptitudes": "node scripts/fetch-gametora.mjs --solo-aptitudes",
    "icons": "node scripts/generate-launcher-icons.mjs",
    "sitio:dev": "npm --prefix sitio run dev",
    "sitio:check": "npm --prefix sitio run check",
    "sitio:lint": "npm --prefix sitio run lint",
    "sitio:test": "npm --prefix sitio run test",
    "sitio:build": "npm --prefix sitio run build"
  }
}
