declare global {
  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface PageState {}
    // interface Platform {}
  }

  /** Versión inyectada por Vite (define __APP_VERSION__). */
  const __APP_VERSION__: string
}

export {}
